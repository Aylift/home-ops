# --- ESP32 Node (host-side sync) ---
ESP_IP=
WEBREPL_PASS=

# --- Postgres (docker-compose) ---
POSTGRES_USER=homeops
POSTGRES_PASSWORD=
POSTGRES_DB=homeops

# --- Backend (local dev, outside Docker) ---
# DATABASE_URL=postgresql+psycopg://homeops:homeops@localhost:5432/homeops

# --- OpenWeatherMap (backend is the single weather authority) ---
# Set WEATHER_ENABLED=true and fill the key + coordinates to enable.
WEATHER_ENABLED=false
OWM_API_KEY=
OWM_LAT=
OWM_LON=
# WEATHER_CACHE_TTL=600
# WEATHER_MAX_STALE=21600
